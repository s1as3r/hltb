[project]
name = "hltb"
version = "0.6.6"
description = "check how long to beat a game"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
    "tabulate>=0.9.0",
]

[project.scripts]
hltb = "hltb.main:main"
